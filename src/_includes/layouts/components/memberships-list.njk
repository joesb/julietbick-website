<section class="memberships">
  <ul class="memberships--list bare-list inline-list inline-list--space-evenly">
    {% for item in metadata.memberships %}
    <li class="memberships--item membership-{{ item.machine_name }}">
      <a href="{{ item.url }}" rel="noreferrer noopener{% if item.rel %} {{ item.rel }}{% endif %}" target="_blank" class="memberships--item-link {{ item.machine_name }}{% if item.no_icon %} social-site--no-icon{% endif %}" title="Member of {{ item.name }}"><span class="hide-text">{{ item.name }}</span></a>
    </li>
    {% endfor %}
  </ul>
</section>