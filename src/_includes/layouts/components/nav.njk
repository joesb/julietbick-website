<nav aria-label="Main navigation">
  <div class="main-nav">
    {# <ul>
    {%- for nav in collections.mainNav -%}
      <li>
        <a href="{{ nav.url | url }}"{% activepath nav.url, page.url %}>{{ nav.data.navtitle }}</a>
      </li>
    {%- endfor -%}
    </ul> #}

    {% set navPages = collections.all | eleventyNavigation %}
    <ul class="main-nav--list">
    {%- for entry in navPages %}
    {% if not (entry.url == "/" or entry.hideMainNav) %}
      <li class="main-nav--list--item">
        <a href="{{ entry.url }}" class="main-nav--list--item-link {% activepath entry.url, page.url %}">{{ entry.title }}</a>
      </li>
    {% endif %}
    {%- endfor %}
    </ul>
  </div>

  {# <div class="secondary-nav">
    <ul>
      {%- for primary in collections.primaryNav -%}
      <li>
        <a href="{{ primary.url | url }}"{% if primary.url == page.url %} data-current="current item" class="current"{% endif %}>{{ primary.data.navtitle }}</a>
      </li>
      {%- endfor -%}
    </ul>
  </div> #}
</nav>